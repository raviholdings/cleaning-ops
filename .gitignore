node_modules/
.env
.env.local
.env.*.local
tmp/
logs/
dist/
dist-preview/
.astro/
apps/*/node_modules/
apps/*/dist/
apps/*/tmp/
apps/*/.astro/
.git/lost-found/
private/
local-secrets/
*.corrupt.bak

# 임시 작업 및 세션 복구 로그 폴더
recovered/
recovered*
scratch/

# 런타임 락 파일 및 바이너리/실행 파일
*.lock
.windows-naver-crawl.lock
*.exe
_binary/.vite/
apps/*/.vite/

# 머신마다 다르게 쌓이는 실행 기록. 추적하면 VM 이 git pull 할 때마다
# "local changes would be overwritten" 으로 막힌다. 계획 파일(reports/*.json)은
# 배치 재현에 필요하므로 계속 추적한다.
reports/haiip-windows-ip-changes.jsonl
reports/naver-login/
reports/verify-debug.png
verify_debug.png
